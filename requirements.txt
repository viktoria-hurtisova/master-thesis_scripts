# Dependencies for Python scripts in the scripts/ directory
#
# Install with: pip install -r scripts/requirements.txt
#
# Note: Most scripts only use Python standard library modules.
# The packages below are only required for analyze_solver_results.py

# Data analysis and visualization
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

